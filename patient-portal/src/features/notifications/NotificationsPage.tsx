import { Bell } from "lucide-react";
import { PlaceholderPage } from "@/components/PlaceholderPage";

export function NotificationsPage() {
  return (
    <PlaceholderPage
      title="Notifications"
      description="Updates from your appointments, messages, and care plan."
      icon={<Bell />}
    />
  );
}
